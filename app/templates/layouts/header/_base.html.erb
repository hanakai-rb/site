<header
  class="
    bg-primary flex items-center gap-4 px-4 lg:px-6 sticky top-0
    text-primary-reversed rounded-b-2xl
  "
  data-defo-size-to-var="<%= { blockVarName: "--hk-nav-height" }.to_json %>"
>
  <a href="/">Hanakai</a>

  <%= render "header/nav_desktop" %>

  <div
    data-defo-docsearch="<%= {
      appId: settings.docsearch_app_id,
      apiKey: settings.docsearch_api_key,
      indexName: settings.docsearch_index_name
    }.to_json %>"
  ></div>
</header>
