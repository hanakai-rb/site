<!DOCTYPE html>

<html
  lang="en"
  class="
    scroll-pt-[calc(var(--hk-nav-height)+var(--spacing)*7)]
    theme-hanami-light
  "
>
  <head>
    <meta charset="UTF-8">

    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>
      <% if content_for(:title) %>
        <%= raw content_for(:title) %>
        &middot; Hanakai
      <% else %>
        Hanakai
      <% end %>
    </title>

    <%= favicon_tag %>
    <%= stylesheet_tag "app" %>
    <%= raw content_for(:meta) %>
    <link
      rel="preconnect"
      href="https://<%= settings.docsearch_app_id %>-dsn.algolia.net"
      crossorigin
    >

    <%= javascript_tag "app", async: true %>
  </head>
  <body
    class="font-sans text-neutral bg-white"
    data-defo-foresight="<%= { elementSelector: "a[href^=/]" }.to_json %>"
  >
    <%= render "header/base", nav_items: nav_items %>

    <div class="mx-auto flex max-w-[1540px] flex-col">
      <main class="px-4 lg:px-6 flex flex-col"><%= yield %></main>
    </div>
  </body>
</html>
