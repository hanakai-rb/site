<!DOCTYPE html>

<html
  lang="en"
  class="scroll-pt-[calc(var(--hk-nav-height)+var(--spacing)*7)] theme-<%= theme %>-light"
>
  <head>
    <meta charset="UTF-8">

    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>
      <% if content_for(:title) %>
        <%= raw content_for(:title) %>
        &middot; Hanakai
      <% else %>
        Hanakai
      <% end %>
    </title>

    <%= favicon_tag %>
    <%= stylesheet_tag "app" %>
    <%= raw content_for(:meta) %>
    <link
      rel="preconnect"
      href="https://<%= settings.docsearch_app_id %>-dsn.algolia.net"
      crossorigin
    >

    <%= javascript_tag "app", async: true, type: "module" %>
  </head>
  <body
    class="font-sans text-neutral bg-white flex flex-col min-h-dvh"
    data-defo-foresight="<%= { elementSelector: "a[href^='/']" }.to_json %>"
  >
    <%= render "header/base", nav_items: header_nav_items %>

    <div class="mx-auto flex w-full max-w-[1540px] flex-col mb-6 lg:mb-8 flex-1">
      <main class="px-4 lg:px-6 flex flex-col flex-1"><%= yield %></main>
    </div>

    <%= render "footer", nav_items: footer_nav_items %>
  </body>
</html>
